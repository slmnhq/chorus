instance:
  host: "gillette"
  port: 5432
  maintenance_db: "postgres"

account:
  db_username: "gpadmin"
  db_password: "secret"

databases:
  - name: "gpdb_test_database"
    schemas:

    - name: "gpdb_test_schema"

      master_tables:
        - name: "master_table1"
          partitions: 7

      external_web_tables:
        - name: "external_web_table1"
          url: "http://intranet.company.com/expenses/sales/file.csv"
          format: 'CSV'

