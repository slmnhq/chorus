admin:
  login: edcadmin
  password: secret

login_route: '/#/login'
new_user_route: '/#/users/new'
homepage: '#/'

user:
  route: '#/users'
  first_name: Joe
  last_name: Blow
  email: '@blow.com'
  password: password

user_menu:
  menu: '.header .username a'
  profile:
  signout: '.menu.popup_username a[href="#/logout"]'

instance:
  route: '#/instances'
  new_btn: 'button[data-dialog=InstancesNew]'
  list: '.instance_list'

workspace:
  route: '#/workspaces'
  new: 'Create Workspace'

gpdb_instance:
  gpdb_host: chorus-gpdb40.sf.pivotallabs.com
  gpdb_port: 5432
  gpdb_user: gpadmin
  gpdb_pass: secret

hadoop_instance: