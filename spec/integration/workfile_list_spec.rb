require File.join(File.dirname(__FILE__), 'spec_helper')

describe "workfile list" do
  it "displays the list correctly from the URL" do
    # this tests that the router correctly passes the matched parameters to the page constructor
    pending 'cannot yet create workfiles to access'
  end

  it "selects an li item when clicked" do
    # this tests that delegateEvents is correctly working in chorus.views.MainContentView.postRender
    pending 'cannot yet create workfiles'
  end
end