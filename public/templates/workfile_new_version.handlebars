<form>
    <textarea class="required" name="commitMessage" placeholder="{{t "workfile.new_version_dialog.commmit_message"}}" maxlength="256" tabindex="1" maxlength="256">{{body}}</textarea>

    <div class="modal_controls">
        <button type="button" class="cancel" tabindex=3>{{t "actions.cancel"}}</button>
        <button type="submit" class="submit" tabindex=2>{{t "workfile.new_version_dialog.button.save"}}</button>
    </div>
</form>