<input class="search" />
<div class="schema_name">{{t "schema.functions.search_in" schemaName=schemaName}}</div>
{{#if models.length}}
    <ul class="list">
        {{#each models}}
            <li data-name="{{functionName}}" title="{{functionName}}">
                <span class="spacer left"/>
                <img src="images/function.png">
                <div class="name">{{functionName}}</div>
                <span class="spacer right"/>
            </li>
        {{/each}}
    </ul>
{{else}}
    <div class="none_found">{{t "schema.functions.none_found"}}</div>
{{/if}}
