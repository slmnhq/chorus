{{#each models}}
    <li data-id="{{id}}">
        <img src="/edc/workspace/{{id}}/image?size=icon"/>
        <a class="name">{{name}}</a>
        {{#if isPublic}}
            <span class="public tag">Public</span>
        {{/if}}
    </li>
{{/each}}
