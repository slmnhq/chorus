<div class="spinner_container">
    <img {{#if hasImage}} src="{{imageUrl}}" {{/if}}/>
</div>
<div class="file-wrapper">
    <a class="action">{{#if hasImage}}{{t changeImageKey}}{{else}}{{t addImageKey}}{{/if}}</a>
    <input type="file"/>
</div>
