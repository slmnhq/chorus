{{#each models}}
    <li>
        <span class="spacer left"></span>
        <div class="text right">
            <div class="main">
                <span class="name">{{name}}</span>
            </div>
            <div class="summary">{{comment}}</div>
        </div>
        <div class="type {{typeClass}}"><span class="arrow_right"/>{{typeString}}<span class="arrow_down"/></div>
        <span class="spacer right"></span>
    </li>
{{/each}}
