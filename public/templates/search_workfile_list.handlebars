{{#if shown}}
    {{#if filteredSearch}}
        <div class="pagination">
            <div class="right">
                {{#if hasNext}}
                    <a href="#" class="next">
                        {{t "search.next"}}
                    </a>
                {{else}}
                    <span class="next">{{t "search.next"}}</span>
                {{/if}}
                |
                {{#if hasPrevious}}
                    <a href="#" class="previous">
                       {{t "search.previous"}}
                    </a>
                {{else}}
                    <span class="previous">{{t "search.previous"}}</span>
                {{/if}}
            </div>
             <span class="count">{{t "search.results" count=total}}</span>
         </div>
    {{else}}
        <div class="details">
            <div class="right">
                {{#if moreResults}}
                    <span class="count">{{t "search.count" shown=shown total=total}}</span>
                    <a class="show_all" data-type="workfile" href="#">{{t "search.show_all"}}</a>
                {{else}}
                    <span class="count">{{t "search.count_short" shown=shown}}</span>
                {{/if}}
            </div>
            <span class="title">{{t "workfiles.title"}}</span>
        </div>
        <span class="title">{{t "workfiles.title"}}</span>
    {{/if}}

    <ul class="workfile_list"></ul>
{{/if}}
