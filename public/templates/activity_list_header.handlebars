<div class="menus">
    <span class="title">{{t "filter.show"}}</span>
    <a href="#" class="all">{{t "filter.all_activity"}}</a>
    <span class="separator">|</span>
    <a href="#" class="insights">{{t "filter.only_insights"}}</a>

    <div class="badge">
        <div class="left"/><div class="center">{{ count }}</div><div class="right"/>
    </div>
</div>

<div class="title">
    {{#if iconUrl}}
        <img src="{{iconUrl}}"/>
    {{/if}}
    <h1 title="{{title}}">{{title}}</h1>
</div>
